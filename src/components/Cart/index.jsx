import { CartBody } from "./CartBody";
import { CartHeader } from "./CartHeader";
import { CartRoot } from "./CartRoot";

export const Cart = {
    Root: CartRoot,
    Header: CartHeader,
    Body: CartBody
}